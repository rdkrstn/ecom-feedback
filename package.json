{
  "name": "sagados-feedback-review",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:web": "cd apps/web && pnpm dev",
    "dev:api": "cd agents && uv run uvicorn sagad_feedback.api:app --reload --port 8000",
    "dev": "concurrently \"pnpm dev:web\" \"pnpm dev:api\""
  },
  "devDependencies": {
    "concurrently": "latest"
  }
}
